export type CreatorType = {
  id: string;
  name: string;
  image: string;
  description: string;
  tags: string[];
  patronCount: number;
}

export type MembershipType = {
  title: string;
  image: string;
  price: string;
  benefits: string[];
  description: string
}